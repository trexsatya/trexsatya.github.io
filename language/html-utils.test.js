import{escapeHtml as e,decodeHtmlEntities as t,removeHtmlTags as o,encodeHtmlTags as s,decodeHtmlTags as n}from"./html-utils";describe("escapeHtml (re-exported)",()=>{test("escapes &<>\"'",()=>{expect(e("<a href=\"x\">'&'</a>")).toBe("&lt;a href=&quot;x&quot;&gt;&#39;&amp;&#39;&lt;/a&gt;")})}),describe("decodeHtmlEntities",()=>{test("decodes named entities",()=>{expect(t("&lt;a&gt;&amp;b&amp;&lt;/a&gt;")).toBe("<a>&b&</a>")}),test("decodes numeric entities",()=>{expect(t("&#39;hello&#39;")).toBe("'hello'")}),test("plain text passes through unchanged",()=>{expect(t("just text")).toBe("just text")})}),describe("removeHtmlTags",()=>{test("strips tags but keeps text content",()=>{expect(o("<b>hello</b> <i>world</i>")).toBe("hello world")}),test("decodes entities after stripping",()=>{expect(o("<p>&amp;foo</p>")).toBe("&foo")}),test("handles self-closing tags",()=>{expect(o("a<br/>b")).toBe("ab")}),test("handles null/undefined gracefully",()=>{expect(o(null)).toBe(""),expect(o(void 0)).toBe("")})}),describe("encodeHtmlTags / decodeHtmlTags round trip",()=>{let e=0;const t=()=>"uuid"+e++;beforeEach(()=>{e=0}),test("replaces each tag with a unique placeholder",()=>{const[e,o]=s("a <b>bold</b> c",t);expect(Object.keys(o)).toEqual(["<b>","</b>"]),expect(e).not.toContain("<b>"),expect(e).not.toContain("</b>"),expect(e).toContain("bold")}),test("decodeHtmlTags restores the original tags (placeholders add padding)",()=>{const[e,o]=s("a <b>bold</b> c",t),c=n(e,o);expect(c.replace(/\s+/g," ")).toBe("a <b> bold </b> c"),expect(c).toContain("<b>"),expect(c).toContain("</b>")}),test("identical tags share one placeholder",()=>{const[,e]=s("<i>a</i> <i>b</i>",t);expect(Object.keys(e)).toEqual(["<i>","</i>"])}),test("text with no tags returns the input unchanged + empty encoding map",()=>{const[e,o]=s("no tags here",t);expect(e).toBe("no tags here"),expect(o).toEqual({})}),test("handles null/undefined",()=>{const[e,o]=s(null,t);expect(e).toBe(""),expect(o).toEqual({})})}),describe("decodeHtmlTags edge cases",()=>{test("returns text untouched when encodings is empty",()=>{expect(n("hi",{})).toBe("hi")}),test("null encodings doesn't throw",()=>{expect(n("hi",null)).toBe("hi")}),test("handles null text",()=>{expect(n(null,{"<b>":" _x_ "})).toBe("")})});