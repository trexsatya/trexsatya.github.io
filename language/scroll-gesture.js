export const GESTURE_SLOP=6;export const FS_RECHECK_ABOVE=8192;export const FS_RECHECK_STEP=512;export function nativeScrollVerdictStale(t,e){const r=Number(t);if(!isFinite(r)||r<=8192)return!1;const n=Number(e);return!isFinite(n)||Math.abs(r-n)>512}export function gestureAxis(t,e,r,n){const o=Number(n)>0?Number(n):6,s=Math.abs(Number(t)||0),u=Math.abs(Number(e)||0);return Math.max(s,u)<o?null:r?u>s?"y":"x":"y"}