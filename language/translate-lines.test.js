import{translateLines as e}from"./translate-lines";describe("translateLines",()=>{test("returns translations in the same order as the input",async()=>{expect(await e(["a","b","c"],async e=>e.toUpperCase())).toEqual(["A","B","C"])}),test("passes whitespace-only lines through untouched (no translateFn call)",async()=>{const t=[],a=await e(["hej","   ",""],async e=>(t.push(e),"X"));expect(a).toEqual(["X","   ",""]),expect(t).toEqual(["hej"])}),test("never runs more than `concurrency` translateFn calls at once",async()=>{let t=0,a=0;await e(["1","2","3","4","5","6","7","8"],e=>new Promise(n=>{t++,a=Math.max(a,t),setTimeout(()=>{t--,n(e)},5)}),{concurrency:3}),expect(a).toBeLessThanOrEqual(3),expect(a).toBeGreaterThan(1)})});