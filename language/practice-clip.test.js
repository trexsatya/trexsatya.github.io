import{clipRange as t,contiguousLineWindow as e,gapSecondsBefore as o,formatGap as s,CLIP_MIN_SECONDS as a}from"./practice-clip";const n=(t,e,o)=>({index:t,start:{ordinal:e},end:{ordinal:o}}),r=[0,1,2,3,4].map(t=>n(t+1,10+t,11+t)),c={start:12,stop:13},p=[n(1,10,11),n(2,11,12),n(3,12,13),n(4,600,601),n(5,601,602)];describe("clipRange",()=>{test("plays the matched line and one more, which is what it always did",()=>{expect(t(r,2,0,0,c)).toEqual({start:12,stop:14})}),test("a line added after is heard, not just shown",()=>{expect(t(r,2,0,1,c).stop).toBe(15)}),test("a line added before moves the start back",()=>{expect(t(r,2,1,0,c).start).toBe(11),expect(t(r,2,2,0,c).start).toBe(10)}),test("with nothing added the captured start wins over the subtitles",()=>{const e=r.map(t=>({...t,start:{ordinal:t.start.ordinal+5}}));expect(t(e,2,0,0,c).start).toBe(12)}),test("falls back to the captured times when the line cannot be placed",()=>{expect(t(r,-1,0,2,c)).toEqual(c),expect(t([],0,0,0,c)).toEqual(c),expect(t(null,0,0,0,c)).toEqual(c)}),test("a range that would not move forward is given a length",()=>{expect(t([],0,0,0,{start:30,stop:30})).toEqual({start:30,stop:30+a})})}),describe("clipRange over a stitched file",()=>{test("does not run across the jump to reach the next line",()=>{const e=t(p,2,0,1,{start:12,stop:13});expect(e.stop).toBe(13),expect(e.stop-e.start).toBeLessThan(5)}),test("does not run back across the jump either",()=>{const e=t(p,3,2,0,{start:600,stop:601});expect(e.start).toBe(600)}),test("still takes context on the side that is continuous",()=>{const e=t(p,2,2,0,{start:12,stop:13});expect(e.start).toBe(10),expect(e.stop).toBe(13)}),test("the run-off is taken when the next line really is next",()=>{const e=t(p,1,0,0,{start:11,stop:12});expect(e.stop).toBe(13)})}),describe("contiguousLineWindow",()=>{test("gives every line asked for when the run is continuous",()=>{expect(e(r,2,1,1)).toEqual({from:1,to:3}),expect(e(r,2,9,9)).toEqual({from:0,to:4})}),test("stops at the seam rather than putting an unplayable line on the card",()=>{expect(e(p,2,0,1)).toEqual({from:2,to:2}),expect(e(p,2,2,5)).toEqual({from:0,to:2})}),test("stops at the seam going backwards too",()=>{expect(e(p,3,3,1)).toEqual({from:3,to:4})}),test("always covers the matched line itself",()=>{expect(e(p,3,0,0)).toEqual({from:3,to:3})}),test("an unplaceable line asks for nothing",()=>{expect(e(r,-1,1,1)).toEqual({from:0,to:-1}),expect(e([],0,1,1)).toEqual({from:0,to:-1})})}),describe("gapSecondsBefore",()=>{test("measures the break, for the marker search results draws",()=>{expect(o(p)).toEqual([0,0,0,587,0])}),test("continuous lines report no break",()=>{expect(o(r)).toEqual([0,0,0,0,0])}),test("lines with no timing are not guessed at",()=>{expect(o([{index:1},{index:2}])).toEqual([0,0]),expect(o(null)).toEqual([])})}),describe("formatGap",()=>{test("reads as a length of video",()=>{expect(s(12)).toBe("12s"),expect(s(60)).toBe("1m"),expect(s(587)).toBe("9m 47s")}),test("nothing to say about no gap",()=>{expect(s(0)).toBe(""),expect(s(null)).toBe("")})});