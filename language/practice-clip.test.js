import{clipRange as t,CLIP_MIN_SECONDS as e}from"./practice-clip";const a=[0,1,2,3,4].map(t=>({index:t+1,start:{ordinal:10+t},end:{ordinal:11+t}})),o={start:12,stop:13};describe("clipRange",()=>{test("plays the matched line and one more, which is what it always did",()=>{expect(t(a,2,0,0,o)).toEqual({start:12,stop:14})}),test("a line added after is heard, not just shown",()=>{expect(t(a,2,0,1,o).stop).toBe(15),expect(t(a,2,0,2,o).stop).toBe(15)}),test("a line added before moves the start back",()=>{expect(t(a,2,1,0,o).start).toBe(11),expect(t(a,2,2,0,o).start).toBe(10)}),test("with nothing added the captured start wins over the subtitles",()=>{const e=a.map(t=>({...t,start:{ordinal:t.start.ordinal+5}}));expect(t(e,2,0,0,o).start).toBe(12)}),test("does not run off the end of the subtitles",()=>{const e=t(a,4,0,3,o);expect(e.stop).toBe(15),expect(t(a,0,5,0,o).start).toBe(10)}),test("falls back to the captured times when the line cannot be placed",()=>{expect(t(a,-1,0,2,o)).toEqual(o),expect(t([],0,0,0,o)).toEqual(o),expect(t(null,0,0,0,o)).toEqual(o)}),test("lines with no timing leave the captured range alone",()=>{expect(t([{index:1},{index:2},{index:3}],1,1,1,o)).toEqual(o)}),test("a range that would not move forward is given a length",()=>{expect(t([],0,0,0,{start:30,stop:30})).toEqual({start:30,stop:30+e})})});