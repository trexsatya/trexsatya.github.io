import{numberRows as t,plainRows as e,parseNumbered as r}from"./translate-block";describe("plainRows",()=>{test("accepts an answer that kept the rows it was given",()=>{expect(e("en\ntvå\ntre",3)).toEqual(["en","två","tre"])}),test("refuses an answer whose row count moved",()=>{expect(e("en två\ntre",3)).toBeNull(),expect(e("allt på en rad",3)).toBeNull()})}),describe("parseNumbered",()=>{test("recovers rows when the line breaks survived",()=>{expect(r("1. Inte oroa dig.\n2. PETE: [skrattar]\n3. Roligt.",3)).toEqual(["Inte oroa dig.","PETE: [skrattar]","Roligt."])}),test("recovers rows when every line break was lost",()=>{expect(r("1. Inte oroa dig. 2. PETE: [skrattar] 3. Roligt.",3)).toEqual(["Inte oroa dig.","PETE: [skrattar]","Roligt."])}),test("survives the translator re-segmenting across rows",()=>{expect(r("1. Inte oroa dig. Låt mig se.\n2. PETE: [skrattar] 3. Roligt.",3)).toEqual(["Inte oroa dig. Låt mig se.","PETE: [skrattar]","Roligt."])}),test("accepts ')' as the marker punctuation too",()=>{expect(r("1) ett 2) två",2)).toEqual(["ett","två"])}),test("keeps an empty row empty",()=>{expect(r("1. ett\n2.\n3. tre",3)).toEqual(["ett","","tre"])}),test("refuses when a marker is missing",()=>{expect(r("1. ett\n3. tre",3)).toBeNull()}),test("does not mistake a number inside the text for a marker",()=>{expect(r("1. på 1500-talet, i 15. året\n2. sedan",2)).toEqual(["på 1500-talet, i 15. året","sedan"])}),test("round-trips with numberRows when nothing is disturbed",()=>{const e=["ett","två","tre"];expect(r(t(e),e.length)).toEqual(e)})});