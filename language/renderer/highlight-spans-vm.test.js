import{buildHighlightSpans as e,sliceByHighlightSpans as t}from"./highlight-spans-vm.js";const n=(e,t)=>e.slice(t.start,t.end);describe("buildHighlightSpans — basics",()=>{test("empty/null pattern → no spans",()=>{expect(e("hello world","")).toEqual([]),expect(e("hello world",null)).toEqual([]),expect(e("hello world","   |   ")).toEqual([])}),test("no match → empty array",()=>{expect(e("hello world","xyz")).toEqual([])}),test("single-token match expands to whitespace boundary",()=>{const t="I love designing webpages",s=e(t,"design");expect(s).toHaveLength(1),expect(n(t,s[0])).toBe("designing")})}),describe("buildHighlightSpans — multi-word phrase priority",()=>{test("phrase wins over its single-word alternatives",()=>{const t="foo x y z bar x baz",s=e(t,"x y z|x|y|z");expect(s).toHaveLength(1),expect(n(t,s[0])).toBe("x y z")}),test("phrase with relaxed spaces matches multiple internal whitespace",()=>{const t="foo i  förväg bar",s=e(t,"i förväg");expect(s).toHaveLength(1),expect(n(t,s[0])).toBe("i  förväg")})}),describe("buildHighlightSpans — phrase fallback to single-word",()=>{test("when no phrase matches, single-word alternations apply (unbounded)",()=>{const t="I love designing webpages",s=e(t,"design|love");expect(s).toHaveLength(2),expect(s.map(e=>n(t,e))).toEqual(["love","designing"])}),test("when no phrase matches, sub-words split from multi-word are BOUNDED",()=>{const t="vi vilken i",s=e(t,"i förväg");expect(s).toHaveLength(1),expect(n(t,s[0])).toBe("i")}),test("overlapping spans merge into a single span",()=>{const t="abcdef",s=e(t,"abc|cdef");expect(s).toHaveLength(1),expect(n(t,s[0])).toBe("abcdef")})}),describe("buildHighlightSpans — defensive",()=>{test("invalid regex source falls through to no spans",()=>{expect(e("hello","(unclosed")).toEqual([])}),test("newlines act as word boundaries during expansion",()=>{const t="foo\ndesign",s=e(t,"design");expect(s).toHaveLength(1),expect(n(t,s[0])).toBe("design")})}),describe("sliceByHighlightSpans",()=>{test("returns single plain chunk when no spans",()=>{expect(t("hi",[])).toEqual([{kind:"plain",text:"hi"}])}),test("returns [] for empty text and no spans",()=>{expect(t("",[])).toEqual([])}),test("interleaves plain + match chunks",()=>{expect(t("foo BAR baz",[{start:4,end:7}])).toEqual([{kind:"plain",text:"foo "},{kind:"match",text:"BAR"},{kind:"plain",text:" baz"}])}),test("handles match at start and end",()=>{expect(t("A middle B",[{start:0,end:1},{start:9,end:10}])).toEqual([{kind:"match",text:"A"},{kind:"plain",text:" middle "},{kind:"match",text:"B"}])})});