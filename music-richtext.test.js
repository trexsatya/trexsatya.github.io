import{safeHtml as e,ALLOWED_TAGS as t}from"./music-richtext.js";describe("safeHtml",()=>{test("keeps the inline tags a script would actually use",()=>{expect(e("play <b>softly</b> then <i>louder</i><br>again")).toBe("play <b>softly</b> then <i>louder</i><br>again")}),test("keeps colour, which is the point of the feature",()=>{expect(e('<span style="color:#c00">forte</span>')).toBe('<span style="color:#c00">forte</span>')}),test("plain text is escaped, so prose about a < b still reads as written",()=>{expect(e('a < b & "c"')).toBe("a &lt; b &amp; &quot;c&quot;")}),test("an unknown tag loses the tag and keeps the words",()=>{expect(e("<div>keep <blink>these</blink> words</div>")).toBe("keep these words")}),test("script and style are dropped whole — their body is code, not prose",()=>{expect(e("before<script>alert(1)<\/script>after")).toBe("beforeafter"),expect(e("<style>body{display:none}</style>hi")).toBe("hi")}),test("event handlers and URL attributes cannot survive",()=>{expect(e('<b onclick="alert(1)" onmouseover="x()">hi</b>')).toBe("<b>hi</b>"),expect(e('<img src=x onerror="alert(1)">')).toBe(""),expect(e('<span style="background:url(http://x/y)">hi</span>')).toBe("<span>hi</span>")}),test("a malformed tag costs the emphasis, never the sentence",()=>{expect(e("<b>unclosed emphasis")).toBe("<b>unclosed emphasis</b>"),expect(e("half a <b tag")).toBe("half a <b></b>"),expect(e("words <em>then a typo <em> and more")).toContain("and more")}),test("empty and nullish input",()=>{expect(e("")).toBe(""),expect(e(null)).toBe(""),expect(e(void 0)).toBe("")}),test("the allowlist is inline-only — no block layout, nowhere for it to go",()=>{["div","table","h1","p","img","a","script"].forEach(e=>expect(t.has(e)).toBe(!1))})});